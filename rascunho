#include <stdio.h>

int fib(int x) {   //sequencia de fibonacci
    if (x == 0) {
        return 0;
    }
    if (x == 1) {
        return 1;
    }
    
    int fib[x + 1];
    fib[0] = 0;
    fib[1] = 1;
    for (int i = 2; i <= x; i++) {
        fib[i] = fib[i - 1] + fib[i - 2];
    }
    return fib[x];
}

int num_calls(int x) {
    
        

int main () {
    int N;
    int x;
    int f, num_calls, result;
    scanf("%d", &N);
    for (int i = 0; i < N; i++) {
        scanf("%d", &x);
        if (1 <= x && x <= 39) {
            for (int j = 0; j < x; j++) {
                f = fib(x);
                num_calls = num_calls
            result = 

            printf("fib(%d) = %d calls = %d\n", x, num_calls, result);
        }
        else {
            printf("ERROR\n");
        }
    }

    return 0;
}





        
