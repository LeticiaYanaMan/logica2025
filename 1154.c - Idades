#include <stdio.h>

int main() {
  int N;
  int S = 0;
  int i = 0;
  do {
    scanf("%d", &N);
    if ( 0 <= N) {
      S = S + N;
      i += 1;
    }
  } while (0 <= N);
  double M = ((double)S / (double) i);
  printf("%.2lf\n", M);
  return 0;
}
