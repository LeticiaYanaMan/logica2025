#include <stdio.h>

int main() {
  int N;
  scanf("%d", &N);
  int resposta;
  for (int i = 1; i <= N; i++) {
    scanf("%d", &resposta);
    printf("resposta %d: %d\n", i, resposta);
  }
  return 0;
}
